package ogame.actions;

import org.openqa.selenium.WebDriver;

public interface GalaxyObjectAction {
    boolean setGalaxyObject(WebDriver webDriver);
    boolean inputCoordinate(WebDriver webDriver);
}
